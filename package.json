{
  "name": "react-micro-fe",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r preview",
    "prepare": "husky"
  },
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "^3.3.3"
  }
}
