{
  "name": "react-micro-fe",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "pnpm install && pnpm -r install",
    "postinstall": "pnpm run build",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r preview",
    "prepare": "husky install"
  },
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "^3.3.3"
  }
}
